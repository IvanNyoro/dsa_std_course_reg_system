# Ivan's Workspace: Core Data Structures
**Role:** Data Structures

### Objectives:
1. **Custom Queue Implementation:** For handling the course waitlist (First-In, First-Out).
2. **Custom Graph/Tree Implementation:** For handling the course prerequisite hierarchy (so that a 1st year doesn't register for a 4th year class) .

###  Files:
* `queue_ds` - Will contain the custom queue logic.
* `prerequisite_graph` - Will contain the node and graph tree logic.
